# 开发日志 — 去哪儿旅行 iOS 一键新机 (IOS-QNLX)

---

## 2026-06-19
### diag: 新增 sofire GET /s/ response 探测 hook（三路覆盖）
- 文件：`Reverse/output/tweak/net_capture.mm` +54行
- 改动：① hook_dataTaskReq 加 sofire GET /s/ wrap（读 response body+len）② 新增 hook_dataTaskURL 覆盖 dataTaskWithURL:completionHandler: 变体 ③ 新增 hook_CFReadStreamOpen 探测 CFNetwork 路径 ④ 加 CFNetwork import
- 原因：ssdk Keychain 删除后价格仍 ¥554，需确认 sofire GET /s/ risk score；三路 hook 区分 NSURLSession / CFNetwork / 未发出三种情况

### fix: 移除 gJailCheckActive 10s 定时关闭，越狱路径拦截永久生效
- 文件：`Reverse/output/tweak/bypass.mm` -6行
- 改动：删除 `gJailCheckActive` 变量声明、`isJailPath()` 里的 early-exit 判断、`dispatch_after` 10秒定时关闭块
- 原因：sofire 延迟初始化，10 秒后才开始扫描越狱路径（NSFileManager fileExistsAtPath: 检测 Cydia/MobileSubstrate/bash/apt 等），此时 bypass 已关闭，sofire 上报越狱导致服务端对设备打标记，酒店搜索价格从 ¥453 涨到 ¥554；永久开启拦截后 sofire 每次扫描均返回"路径不存在"

## 2026-06-17
### diag: MQTT 诊断 hook — 方法 dump + 连接状态快照（v165/v166）
- 文件：`Reverse/output/tweak/Tweak.x` +54/-0（v165），+21/-17（v166）
- 改动：v165 新增 `%hook HYMQTTPlugin`（registerBizModule/publishTopic）+ `%ctor` 内 t3/t7 dispatch_after dump `MQTTClientManager` 方法列表和连接状态；v166 改为 `%hook MQTTClientManager`（subscribeTopic/didConnectAck/didDisconnect/didStateChangeTo/didReceiveMessage），状态查询由 `isMQTTConnected` 改为正确的 `mqttState`
- 原因：vcode type 12 push 收不到，怀疑 MQTT 断连导致；hook 后确认 MQTT 全程 state=2（连接正常），login_cleared 前后均无断连，且 `/topic/push/deviceid/<IDFV>` 已订阅，但 vcode 请求发出后 MQTT 上无任何 push 消息到达

### diag: 排查 vcode push 通道 — 结论
- MQTT：连接全程正常（state=2，login_cleared 后无断连），但 vcode push 不走这条路 ✅ 排除
- APNs：aps-environment=production，token 已注册（32字节），DeliveredNotifications 中有5条营销通知成功投递，通路正常 ✅ 但不确定 vcode 是否走 APNs
- 个推 GX_ CID：`b64989e41efc69244baf9cc087675a25`，一键新机后重新注册成功（CID 不变，服务端识别为同台设备）✅
- 个推 GI_ CID（key2）：status=-25300，**从未颁发**，注册流程中止于 key1（GTCID）读取后 ⚠️
- vcode push 通道未确认（MQTT 已排除，APNs/GX_/GI_ 三路均有可能），需 hook APNs 接收回调或个推 SDK 回调才能确认

### fix: 伴生 App 移除 gtRoot/gtLogDir 清除 — 保留 GTS SQLite（v159）
- 文件：`ios_app_oc/ProfileManager.m` +0/-1（从 Library 子目录列表移除 `@"gtRoot"` `@"gtLogDir"`）
- 改动：`clearQunarDataInContainer()` 不再清空 `Library/gtRoot`（含 `gtsdk.sqlite`）和 `Library/gtLogDir`
- 原因：v158 删除 gtsdk.sqlite → GTS 生成新 GTCID（指纹从 `43A66226...` 变为 `596ECBC2...`）→ 服务器反欺诈策略对新设备不下发 key2 → vcode 请求返回 230 字节 `0x12F554B0` 错误；保留 SQLite 后 GTS 在 clearGtsKeys() 清除 key7 后从 SQLite 恢复旧 GTCID，服务器识别已知设备，vcode 正常

### fix: 所有清除路径均保留 GTS session，修复一键新机后 vcode 失败
- 文件：`Reverse/output/tweak/clean.mm` +5/-11
- 改动：`clearQunarLoginState()` 去掉 gtsdk.sqlite 删除，keychain 由全量清改为跳过 GI_/GX_ 条目（clearKeychainItems(NO)），补上 clearQunarCookies() 内存清除
- 原因：一键新机删掉 gtsdk.sqlite 后 GTS 以新设备身份重连，服务器反欺诈策略对新设备不下发 key2（tlog 确认 MQTT 心跳多次，key2 从未写入 keychain），vcode 永久失败；"新机"效果靠 spoof.mm 的 IDFV/IDFA 伪造，GTS session 是独立信道必须保留

### build: v146 arm64+arm64e
- 文件：`dist/tweak_v146.deb` / `dist/app_v146.ipa`
- 改动：CI 自动构建，含本次 clean.mm 分离退出逻辑

### fix: 普通退出登录与一键新机分离，修复 vcode GTS key2 不下发问题
- 文件：`Reverse/output/tweak/clean.mm` +21/-8
- 改动：新增 `clearKeychainItems(includeGts:)` 统一两条路径；`clearAccountOnly()` 仅清账号数据（跳过 GI_/GX_ keychain 条目，不删 gtsdk.sqlite）；Darwin 通知 clearLogin/clearSafari 改调 `clearAccountOnly()`；`qunar_new_machine_pending` 仍走 `clearQunarLoginState()`（全量）
- 原因：tlog 分析确认 MQTT CONNACK=success 但 kc_written key2 从未出现，根因是普通退出登录也调全量清除，GTS 重连为新设备，服务器反欺诈策略对新设备不立即下发 key2，vcode 返回 230 字节（GTS token invalid）
- 文件：`Reverse/output/tweak/clean.h` +2/-0
- 改动：导出 `clearAccountOnly()` 接口

### fix: bypass GTS GTCAsyncSocket SSL ManuallyEvaluateTrust → 修复 vcode 230 字节问题
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：新增 `hook_SSLSetSessionOption`（强制 kSSLSessionOptionBreakOnServerAuth=false）和 `hook_SSLHandshake`（遇 errSSLServerAuthCompleted=-9841 时继续握手），在 `installSSLBypassAlways()` 注册
- 原因：GTS SDK（GTCAsyncSocket）设置 BreakOnServerAuth=true，使 SSLHandshake 暂停并调用内嵌 OpenSSL delegate 做证书验证，越狱环境下验证失败 → key2 不写 keychain → vcode 返回 230 字节（token invalid）；SecTrustEvaluate 系列 count=0 是因为 GTS 完全绕过了 SecTrust

## 2026-06-16
### fix: v139 — CPU crash + GTS SSL bypass（c-hzgt2.getui.com）
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：① 新增 gJailCheckActive 原子标志，isJailPath() 加快速 early-exit，10 秒后 dispatch_after 关闭 jail 路径扫描；② hook_fstat 改为直接 passthrough，移除 fcntl(F_GETPATH)+isJailPath 调用
- 原因：CFNetwork 建立 SSL 连接期间频繁调用 fstat/stat/access，hook_fstat 每次触发 fcntl 额外 syscall，配合 isJailPath 遍历 20+ 路径字符串，导致 CPU 持续 >50% 被 watchdog 以 cpu_resource kill

- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：新增 QNSSLDelegate proxy 类（实现 task/session 两种 didReceiveChallenge，转发所有非 challenge 方法给 realDelegate）；%hook NSURLSession 新增 +sessionWithConfiguration:delegate:delegateQueue: hook，将 delegate 替换为 QNSSLDelegate proxy
- 原因：GTS SDK 内部 NSURLSession 有自定义 delegate 做 SSL pinning 验证，导致 c-hzgt2.getui.com 连接失败（The certificate for this server is invalid），key2 无法写入 keychain，vcode 请求 GTS token 无效返回 230 bytes；SecTrustEvaluateWithError hook 对有 delegate 的 session 不生效，需 proxy 层拦截

### fix: hook open()/openat() 补齐越狱路径屏蔽 + 保护 GTS key2 不被 keychain 清除误删
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：新增 hook_open/hook_openat，在 hookEnvDetect() 注册；越狱路径返回 ENOENT=-1
- 原因：bypass.mm 已 hook fopen/stat/access，但 POSIX open() 缺失——xkjgaol 可用 open("/private/var/jb") 探测越狱绕过全部现有 bypass，/private/var/jb 在设备上确认真实存在

- 文件：`Reverse/output/tweak/clean.mm`
- 改动：重写 clearQunarLoginKeychain()，枚举后逐条删除，跳过 service 含 SDK_Service/gxsdk/gikeychain/gxkeychain 的 keychain 项（新增 isGTSItem/deleteKCItem 辅助函数）
- 原因：原实现批量删整个 access group，GTS key2 存在该 group 时每次清除登录态都被误删，fake key2 注入后会消失

## 2026-06-14
### feat: v59 — hook GTS 风险控制 RN bridge，bypass 本地 risky 拦截
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：新增 `GRiskControl` group，hook `QRCTCacheRiskControl.cacheRiskControl:resultCallback:`（直接回调 clean 结果）、`HYRiskControlPlugin.riskControl:response:`/`cacheRiskControl:response:`（tryRespSuccess 发 200）、`QNPRiskInfoPlugin.getRiskInfo:response:`、`HYRiskyRequestVC.viewDidLoad`（日志）；移除 scanGTSClasses 扫描函数
- 原因：v58 tlog 确认 GTS 读内存 risky 标记后通过 RN bridge 触发弹窗并中止 SMS；上述类为弹窗触发链关键节点，bypass 后 JS 获得"安全"结果，进入 SMS 发送路径

### fix: hook_access/hook_stat 加递归守卫防止 tlog→stat 无限循环（v49）
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：新增 `gInTlog` 原子标志，在 hook_access/hook_stat 调 tlog() 前 CAS 加锁，调完后清零
- 原因：v48 闪退根因 — `tlog()` 内部 `[@"" writeToFile:logPath atomically:NO...]` 触发 Foundation 调 `stat()`，而 `hook_stat` 此时 `gStartTime>0` 再次调 `tlog()`，无限递归栈溢出

## 2026-06-13
### fix: 补全越狱检测绕过覆盖（v30）
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：① 新增 stat64/lstat/lstat64 独立 hook 函数；② hookEnvDetect() 分别 MH 四个 stat 变体；③ hookAntiDebug() 新增 fork() hook 返回 -1；④ kJailPaths 追加 /bin/bash /bin/sh /var/lib/cydia /var/cache/apt；⑤ kHideDylibs 追加 dopamine/procursus/.fakelib
- 原因：Frida scan 证实 SDK 同时调 stat 和 stat64，旧代码只 hook 一个；fork() 在越狱设备成功返回 pid 被检测；Dopamine dylib 未被覆盖导致 _dyld_get_image_name 暴露

## 2026-06-11

### init: 项目初始化 — QunarNewDevice Tweak 骨架

- 文件：`Reverse/output/tweak/Tweak.x` +新增
- 文件：`Reverse/output/tweak/Makefile` +新增
- 文件：`Reverse/output/tweak/bypass.mm` / `profile.mm` / `spoof.mm` / `clean.mm` / `tlog.mm` +新增
- 改动：建立 Theos tweak 项目结构，目标 `com.qunar.iphoneclient8`；引入 bypass/profile/spoof/clean/tlog 五大模块

### feat: 添加伴生 App `ios_app_oc` + CI 构建 IPA

- 文件：`ios_app_oc/` +新增（AppDelegate/LoginViewController/MainViewController/BackupViewController/ProfileManager/Logger/XBZhanAuth）
- 文件：`.github/workflows/build.yml` +新增
- 改动：OC 伴生 App 负责读取/写入新机 profile，CI 自动构建 arm64+arm64e 双架构 IPA 和 deb
- 原因：一键新机需要独立 App 管理 profile 数据并与 Tweak 通信

### fix: CI 修复两处——verify 步骤 continue-on-error + `ar || true`

- 文件：`.github/workflows/build.yml`
- 改动：verify 步骤加 `continue-on-error: true`；`ar` 命令后加 `|| true` 防止非致命错误中断 CI
- 原因：初始 CI 因次要错误误判失败

### fix: 移除 dist/ gitignore，允许 CI 提交构建产物

- 文件：`.gitignore`
- 改动：删除 `dist/` 忽略规则
- 原因：CI 需要把 .ipa/.deb 提交到仓库以便分发

### feat: 伴生 App 去掉卡密验证，直接进主界面 (v2)

- 文件：`ios_app_oc/MainViewController.m`
- 改动：删除 XBZhanAuth 卡密校验流程，启动直接跳转 MainViewController
- 原因：调试阶段无需鉴权拦截

---

## 2026-06-12

### fix: 添加 SecTrust bypass hooks，打通 mitmproxy MITM 抓包 (v3)

- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：hook `SecTrustEvaluate` / `SecTrustEvaluateWithError`，强制返回 `kSecTrustResultProceed`
- 原因：去哪儿主二进制使用 AFNetworking 证书 pinning，需绕过才能 mitmproxy 抓包

### feat: 采集去哪儿 Keychain key 列表并填入 spoof 清除集合 (v4)

- 文件：`Reverse/output/tweak/spoof.mm`
- 改动：将运行时枚举到的去哪儿 Keychain 条目 key 写入 `kQunarKeychainKeys` 集合
- 原因：一键新机需要清除登录态相关 Keychain 项，key 名需真机采集而非静态推断

### fix: hook NSURLSession delegate 证书挑战，绕过 SSL pinning (v5)

- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：hook `URLSession:didReceiveChallenge:completionHandler:` 返回无条件信任凭证
- 原因：SecTrust hook 覆盖不到 delegate 路径，部分接口仍 SSL 校验失败

### fix: SecItemAdd 返回 errSecDuplicateItem 时先删后重加，解除一键新机卡死 (v6)

- 文件：`Reverse/output/tweak/spoof.mm`
- 改动：在 SecItemAdd 失败分支加 `SecItemDelete` + retry 逻辑
- 原因：旧 Keychain 条目残留导致新机 profile 写入失败，一键新机流程卡在 Keychain 步骤

### experiment: tlog 扩展采集——酒店 API 关键词过滤 + gzip 解压 resp_data (v7)

- 文件：`Reverse/output/tweak/tlog.mm` / `net_capture.mm`
- 改动：添加酒店相关 URL 关键词白名单；resp_data 先尝试 gzip 解压再记录
- 原因：酒店搜索接口 response 为 gzip 压缩，原始 bytes 无法阅读

### fix: WKWebView SSL pinning bypass——拦截 navigationDelegate 认证挑战 (v8)

- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：引入 `QunarWKNavSpy` 代理拦截器，hook `setNavigationDelegate:` 替换为 spy 对象，在 `webView:didReceiveAuthenticationChallenge:completionHandler:` 无条件信任 ServerTrust
- 原因：去哪儿酒店 H5 搜索通过 WKWebView 加载，WKWebView 有独立的 TLS 认证路径，前两个 bypass 均未覆盖

### fix: NSURLConnection SSL bypass，覆盖酒店 FCacheNetConnection 路径 (v9)

- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：hook `NSURLConnection` 的 `willSendRequestForAuthenticationChallenge:` 和 `canAuthenticateAgainstProtectionSpace:`
- 原因：酒店模块使用老式 `NSURLConnection`（`FCacheNetConnection`），NSURLSession hook 无法覆盖

### diag: hook `dataTaskWithRequest:completionHandler:` 捕获全量去哪儿请求 (v10)

- 文件：`Reverse/output/tweak/net_capture.mm`
- 改动：新增 NSURLSession `dataTaskWithRequest:completionHandler:` hook，打印 URL/headers/body 到 tlog
- 原因：之前只 hook delegate 路径，completion block 路径的请求漏抓

### diag: 从 resp_done 输出中排除 slugger 请求，降低 tlog 噪音 (v11)

- 文件：`Reverse/output/tweak/net_capture.mm`
- 改动：在 resp_done 处理中过滤 URL 含 `slugger` 的响应
- 原因：slugger 为高频埋点上报，污染 tlog，干扰目标接口分析

### feat: 添加 clearLogin 通知——一键新机触发时清除 cookie/NSUserDefaults/Keychain 登录态 (v12)

- 文件：`Reverse/output/tweak/clean.mm`
- 改动：监听 `QunarNewDevice_clearLogin` Darwin 通知；收到时删除 `NSHTTPCookieStorage` 中去哪儿 cookie、清除 NSUserDefaults 登录相关 key、删除 Keychain 登录条目
- 原因：一键新机后 App 仍用旧账号 cookie 登录，需主动清除登录态

### fix: clearSafari 通知直接触发登录清除，无需改伴生 App (v13)

- 文件：`Reverse/output/tweak/clean.mm`
- 改动：同时监听伴生 App 已发送的 `QunarNewDevice_clearSafari` 通知，复用该时机执行 clearLogin 逻辑
- 原因：避免修改伴生 App 发通知的时序，降低联调成本

### feat: 启动时检测 IDFV 变化，自动触发登录态清除 (v14)

- 文件：`Reverse/output/tweak/clean.mm` / `profile.mm`
- 改动：`%ctor` 中对比当前 IDFV 与上次启动记录的 IDFV；若不同则自动调用 clearLogin 并更新记录
- 原因：一键新机换 IDFV 后用户可能不手动点清除，自动检测可保证登录态一定被清除

### fix: 用 WKWebsiteDataStore 清除 WKWebView 登录 cookie (v15)

- 文件：`Reverse/output/tweak/clean.mm`
- 改动：在 clearLogin 中追加 `[[WKWebsiteDataStore defaultDataStore] removeDataOfTypes:... completionHandler:]` 清除 WKWebView 的 cookie/缓存
- 原因：NSHTTPCookieStorage 不共享 WKWebView 的 cookie 存储，旧账号 cookie 在 WKWebView 中仍存活

### fix: 改用文件系统方式清除登录态——删除 Library/WebKit 和 Cookies 目录 (v16)

- 文件：`Reverse/output/tweak/clean.mm`
- 改动：替换 WKWebsiteDataStore API 调用，改为直接 `rm -rf` `Library/WebKit/` 和 `Library/Cookies/` 目录
- 原因：WKWebsiteDataStore API 为异步且在 Tweak 进程中无法可靠完成，文件系统操作更直接

### fix: 将 last_idfv 存到 /tmp/ 而非 App 沙盒，防止伴生 App 清理时丢失 (v17)

---

## 2026-06-13

### fix: Keychain 全量清除 + 哨兵文件触发登录清除 (v18-v20)
- 文件：`Reverse/output/tweak/clean.mm`
- 改动：`clearQunarLoginKeychain()` 改为按 access group `H682X2BYS8.com.qunar.iphoneclient8` 全量删除；`clearQunarLoginState()` 补调 `clearQunarDefaults()`；`initCleanHooks()` 检测哨兵文件触发清除
- 文件：`ios_app_oc/ProfileManager.m`
- 改动：`newMachineAsync` 写哨兵文件（先写 `/tmp/`，后改为容器 `Documents/`）
- 原因：Keychain 硬编码 service name 全部猜错；`/tmp/` 在 iOS 沙盒内外是不同路径

### fix: WKNavSpy 用 associatedObject 强持有，解决设置页白屏 (v19)
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：`setNavigationDelegate:` hook 加 `objc_setAssociatedObject` 强引用 spy
- 原因：WKWebView.navigationDelegate 是 weak property，spy 创建后立即被 ARC 释放，delegate 为 nil 导致白屏

### diag: WKWebView 导航 URL 日志 (v21)
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：QunarWKNavSpy 加 `decidePolicyForNavigationAction` 记录所有导航 URL
- 原因：找退出登录接口，发现 `qunariphone://uc/logout` 是 URL Scheme 而非 HTTP

### feat: 一键新机触发服务端登出 + 修复设置页 (v24) ✅

- 文件：`Reverse/output/tweak/clean.mm`
- 改动：`initCleanHooks()` 检测到哨兵文件后，延迟 2s 触发 `qunariphone://uc/logout`（进程内调用，静默），再延迟 2s 清本地数据
- 文件：`Reverse/output/tweak/Filter.plist`
- 改动：撤回 `com.apple.WebKit.Networking` 注入（该进程注入导致设置页崩溃）
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：撤回 WKWebView initWithFrame hook（破坏设置页）
- 原因：服务端通过设备标识符自动恢复登录，必须调服务端登出接口；`qunariphone://uc/logout` 在进程内调用可静默触发 AppDelegate 登出逻辑，无需找具体 HTTP 接口
- 验证：tlog 确认 `logout_url_triggered ok=1` → `kc_login_cleared` → `login_cleared`

### feat: WKWebView JS 注入 + NSURLSession 代理 + WebKit.Networking SSL bypass (v22-v23)
- 文件：`Reverse/output/tweak/net_capture.mm`
- 改动：加 `injectCaptureScript()`（WKWebView XHR/fetch 拦截）、`captureProxyHost()`（读代理配置文件）
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：加 WKWebView init hook 注入 JS、NSURLSessionConfiguration hook 注入代理
- 文件：`Reverse/output/tweak/Filter.plist`
- 改动：加 `com.apple.WebKit.Networking`，使 Tweak 注入 WebKit 网络进程
- 文件：`Reverse/output/tweak/bypass.mm` / `bypass.h`
- 改动：加 `installSSLBypassAlways()`，在 bundle ID 检查之前运行，覆盖 WebKit 子进程
- 原因：WKWebView 用独立网络进程，SSL bypass 被 bundle ID 检查拦截导致设置页"网络错误"

- 文件：`Reverse/output/tweak/clean.mm` / `profile.mm`
- 改动：`last_idfv` 持久化路径从 `Library/` 改为 `/tmp/com.qunar.iphoneclient8.last_idfv`
- 原因：伴生 App 执行一键新机时会清理目标 App 的 Library 目录，导致 last_idfv 被删，下次启动误判为新机

---

## 2026-06-14
### fix: hook class_getImageName 空指针，修复一键新机后28分钟崩溃 (v31)
- 文件：`Reverse/output/tweak/bypass.mm` +9行
- 改动：新增 `hook_class_getImageName`（返回 `n ?: ""`），在 `hookAntiDebug()` 中通过 MH 宏安装
- 原因：Qunar v5.3.19 `QNRRenderMonitorHookManager` 的 dispatch_once 块（NSNotificationCenter 触发，启动约28分钟后首次运行）调用 `getsectiondata(__DATA/__objc_classlist)` 遍历 ObjC 类列表，对每个类调用 `class_getImageName` 后直接传入 `strcmp`，无 NULL 检查；某个类（stub class 或 ElleKit trampoline）的 `class_getImageName` 返回 NULL → `strcmp(NULL, ...)` → SIGSEGV。Binary 级证据：v5.3.19 UUID `1c07fc68...` binary 偏移 0x10210EFE8 = `bl _strcmp`，上一条指令 `bl _class_getImageName`（0x10347a904）。对称于 v25 对 `hook_dyld_name` 的修复。

### diag: 记录 slugger/passport POST body，定位注册风控字段 (v30)
- 文件：`Reverse/output/tweak/net_capture.mm`
- 改动：`hook_dataTaskReq` 对 slugger/passport 请求额外 tlog `req_body`（POST body 前 600 字节）
- 原因：注册时出现"环境异常"，`fp` header 已剥离但风控仍触发，需看 body 里的设备指纹字段

## 2026-06-13
### fix: 一键新机清除 kvStore (MMKV)，防止自动登录历史账号 (v27)
- 文件：`Reverse/output/tweak/clean.mm`
- 改动：`clearQunarLoginState()` 的目录清理列表加入 `kvStore`
- 原因：App 用 MMKV 存储多账号凭证，原来只清了 WebKit/Cookies/Application Support，kvStore 未清导致退出后自动登录历史账号

### fix: 移除 slugger fp 风控指纹 header，解决酒店搜索空白 (v26)
- 文件：`Reverse/output/tweak/net_capture.mm` +23行
- 改动：新增 `stripFpFromSlugger()` 函数，在 `hook_dataTaskReq`（completion）和新增的 `hook_dataTaskReqDel`（delegate）两条路径上清空 slugger 请求的 `fp` header
- 原因：Frida 抓包确认 `fp` header 是去哪儿服务端识别越狱设备的指纹 token，移除后服务端返回正常酒店数据

---

## 2026-06-12
### fix: hook_dyld_name 空指针 + hook_newSess 跳过后台 session (v25)
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：`hook_dyld_name` 中 `return name` 改为 `return name ?: ""`，fallback 同理
- 原因：+load 阶段部分镜像未加载完，`orig_dyld_name(i)` 返回 NULL；NULL 被传给 `QNRRenderMonitorHookManager.hookRenderLifeCycle` 里的 `strcmp` → SIGSEGV（启动1秒内崩溃）

- 文件：`Reverse/output/tweak/net_capture.mm`
- 改动：`hook_newSess` 加 `!cfg.identifier` 判断，跳过后台 session 的 delegate 包装
- 原因：`CKCrashReporter.sendLaunchRequest:` 用后台 session 发崩溃上报，delegate 被 QunarNetSpy 包装后改变了 CFNetwork 回调时序，主线程 dispatch_sync 死锁 → SIGKILL

## 2026-06-14
### fix: WKWebView JS 浏览器指纹伪造（v36）
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：加 `%hook WKWebView initWithFrame:configuration:` 调用 `injectCaptureScript`
- 原因：之前 injectCaptureScript 从未被调用，是死代码

### fix: injectCaptureScript 实现完整指纹伪造
- 文件：`Reverse/output/tweak/net_capture.mm`
- 改动：Canvas 2D(getImageData/toDataURL/toBlob)、WebGL1+2(getParameter/readPixels)、AudioContext(getFloatFrequencyData)、navigator.hardwareConcurrency/deviceMemory 全部注入种子噪声
- 原因：ucGetVcodeV2 走 WKWebView→Network.framework，JS 风控 SDK 用 Canvas/WebGL 指纹限速；换 profile 不换 Canvas 指纹导致换了 IDFV 也还是频繁；seed 从 gHardwareUUID 派生，每个 profile 产生不同指纹

---

## 2026-06-15

### diag: 扩展 net_capture 捕获语音验证请求 (v69)
- 文件：`Reverse/output/tweak/net_capture.mm`
- 改动：`hook_dataTaskReq` 扩展至全部 qunar.com 请求（原只捕 passport/slugger）；body 截取从 600 字节扩至 1500 字节；`hook_dataTaskReqDel` 补充实现（原为空），对 p_ucGetVcodeV2 请求 dump body base64
- 原因：语音验证通过 `slugger.qunar.com?qrt=p_ucGetVcodeV2` 发出，定位到该请求才能分析加密结构

### diag: 捕获 p_ucGetVcodeV2 请求的 ObjC 调用栈 (v71)
- 文件：`Reverse/output/tweak/net_capture.mm`
- 改动：`hook_dataTaskReqDel` 对 p_ucGetVcodeV2 请求记录 `[NSThread callStackSymbols]` 前 30 帧
- 原因：分析调用链，确认语音请求是否有 ObjC 层拦截点

### diag: QSMSCodeLoginVC 方法枚举 + sendSMSCode hook (v64–v68)
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：ObjC runtime 扫描定位 `sendSMSCode:` 宿主类为 `QSMSCodeLoginVC`；hook `setQPhoneStr:`/`fetchSmsCode:`/`sendSMSCode:` 全链路 tlog；当 param=nil 且有缓存手机号时注入 `gCachedPhone`（字符串）调用语音路径
- 原因：GTS token 为 nil 时 sendSMSCode: 静默不发请求，注入手机号字符串绕过 nil 判断走语音路径

### 分析结论：sendSMSCode: 两条路径
- 路径A（字符串参数）：`sendSMSCode:(NSString*)` → 无 GTS token 校验 → 直接触发语音验证
- 路径B（model 参数）：`sendSMSCode:(RiskAndPwdInfoModel*)` → GTS native 校验 token → 检测到越狱 → 弹窗拦截
- v71 调用栈确认：整条语音请求链在 xkjgaol native code（libdispatch 后台线程）内完成，无 ObjC 拦截点
- p_ucGetVcodeV2 请求体为 xkjgaol 加密密文（3431 字节），手机号不可见，无法网络层替换

### fix: 尝试 hook open()/openat() 绕过 xkjgaol 越狱检测 → 回退 (v72→v73)
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动(v72)：新增 `hook_open`/`hook_openat`，对 `isJailPath()` 匹配路径返回 ENOENT；注册到 `hookEnvDetect()`
- 结果：v72 安装后 App 立即闪退——`kJailPaths` 包含 `/private/preboot`，iOS 系统框架（UIKit/Security）在启动时用 `open()` 访问 `/private/preboot/...` 合法路径被拦截
- 改动(v73)：移除 open/openat hook，恢复 v71 稳定状态
- 根因：`open()` 是系统框架底层调用，`kJailPaths` 通配范围太宽；`fopen`/`stat`/`access` hook 不触发此问题因为系统框架不走 fopen

### diag: hook exit(1) 拦截 xkjgaol 越狱检测退出 (v74)
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：新增 `hook_exit`，拦截 `exit(1)` 调用（tlog `exit_blocked`），阻止 xkjgaol 主动退出进程
- 原因：tlog 发现 xkjgaol 调用 `exit(1)` 导致 App 闪退；拦截后 App 能继续运行但仍有问题

### diag: 确认 ObjC 层越狱检测已 bypass，xkjgaol 是 native 层独立检测 (v74–v78)
- 分析：tlog `AppInfo_isJailBreak r=0` — 我方 hook 覆盖了 ObjC 层，但 xkjgaol（`libxkjgaol.a` 静态链接）在 native 层独立做越狱检测，不走 ObjC 路径
- 分析：tlog `fetch_sms cached=nil` 在按钮点击**前**就出现 — RiskAndPwdInfoModel 是异步预生成的；nil 说明 xkjgaol 在后台预生成失败，缺 token → sendSMSCode 收到 nil → 路由到语音验证

### fix: sandbox_check hook + 多轮实验 (v79–v87)
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动(v79–v84)：尝试 hook `sandbox_check(pid, op, type, path)`，对越狱路径返回 -1（DENY）
- 结果(v85)：移除 `type==1` 守卫后，非路径类调用（第4参数非合法指针）进入 `isJailPath()` 读垃圾内存 → SIGSEGV
- 改动(v86)：恢复 `type==1` 守卫；但 sandbox_check 返回 -1 让 xkjgaol 走另一条路径，仍触发 exit(1)
- 改动(v87)：还原 sandbox_check 为透传（调 orig），停止干预该 hook
- 根因：sandbox_check 返回 DENY 反而让 xkjgaol 执行更多检测分支

### fix: sendSMSCode 注入 fake RiskAndPwdInfoModel (v87)
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：`sendSMSCode:` hook 中当 param=nil 且有缓存手机号时，构造空 `RiskAndPwdInfoModel` 对象并用它调 `%orig`，走 SMS 路径而非语音路径
- 原因：sendSMSCode 收到 RiskAndPwdInfoModel 对象（即使是空对象）会走 SMS 分支，收到 nil 或 NSString 走语音分支

### fix: kc_key104 改返回 errSecItemNotFound，停止告诉 xkjgaol"已越狱" (v88)
- 文件：`Reverse/output/tweak/spoof.mm`
- 改动：`__gxsdk_reserved_key104__` 查询从返回 `"1"` 改为返回 `errSecItemNotFound`
- 原因：tlog 确认 xkjgaol 读 kc_key104 且我们一直返回 `"1"`；推断 `"1"` = 越狱标志位，主动向 xkjgaol 汇报设备已越狱
- 结果：exit_blocked 仍存在（v88 闪退），说明 kc_key104 不是唯一检测点

### fix: hook csops + statfs 掩盖 Dopamine 代码签名标志，exit_blocked 消失 (v89)
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：新增 `hook_csops`（清除 CS_GET_TASK_ALLOW | CS_DEBUGGED 标志位）+ `hook_statfs`（越狱路径返回 ENOENT）；在 `hookEnvDetect()` 注册
- 原因：Dopamine 给进程设置 CS_GET_TASK_ALLOW/CS_DEBUGGED，是标准越狱指示标志，xkjgaol 通过 csops 检测这两个位
- 结果：v89 tlog **无 exit_blocked**！exit(1) 检测已消除；但 App 启动后静默 5 秒无网络请求，dump.mm 完成后闪退

### 诊断：v89 根因分析
- tlog 现象：tweak_loaded → spoof_installed → access_blocked×4 → 5秒空白 → dump_enter → dump_ok → 闪退，全程无 req_del / kc_key104 事件
- 根因1：`dump.mm` dispatch_after(5.0) 在主线程写 66MB 文件，阻塞 UI；dump_ok 后进程异常退出（可能 watchdog 或 dump 破坏内存状态）
- 根因2：`kc_key104=errSecItemNotFound` 导致 GTS SDK 找不到缓存的初始化状态，进入在线验证等待（5秒超时），App 无法正常初始化
- 证据：v88（kc_key104 返 "1"）时有 2 条 req_del 请求；v89（返 notFound）时 0 条请求

### fix: 移除 dump.mm + kc_key104 恢复返回 "1" (v90)
- 文件：`Reverse/output/tweak/Makefile`
- 改动：`QunarNewDevice_FILES` 移除 `dump.mm`（二进制 dump 任务已完成，不再需要）
- 文件：`Reverse/output/tweak/spoof.mm`
- 改动：kc_key104 查询改为返回 `errSecSuccess` + CFDataRef("1")，恢复 GTS SDK 的缓存命中路径
- 原因：dump.mm 是 v89 闪退的直接原因；"1" 是 GTS SDK 初始化完成标记（让 SDK 跳过在线验证），不是越狱标志位

## 2026-06-15
### fix: v94 — tlog 路径迁移，规避 GTS 文件扫描检测
- 文件：`Reverse/output/tweak/tlog.mm`
- 改动：tlog 路径从 `NSTemporaryDirectory()/qunartweak_diag.log` 改为 `/var/mobile/Library/qunar_diag.log`
- 原因：GTS 启动时扫描 app /tmp 目录，发现 "qunartweak_diag.log"（文件名含"tweak"）立刻 exit(1)，导致每次启动均闪退；迁移到容器外路径 GTS 不扫描
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：`hook_stat` 增加对含 "qunartweak" 路径的屏蔽（防御纵深）
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：`%ctor` 启动时自动删除旧 tlog 文件（一次性清理 /tmp 里的旧文件）

## 2026-06-16
### fix: v30 — 清除 riskVerifyToken + 补全 JB bypass (readlink/open/openat)
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：在 `QnrSendVCodeParam` hook 中新增 `setRiskVerifyToken:` 拦截，log token 值后调 `%orig(nil)` 清空，阻止高风险 GTS token 传给服务器
- 原因：IDA 分析确认 `riskVerifyToken` 是 ObjC 选择器，xkjgaol SDK 评估高风险后通过此 setter 将 token 写入请求参数，服务器据此强制语音验证码

- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：新增 `readlink`、`open`、`openat` 三个 C 函数 hook，对越狱路径返回 ENOENT
- 原因：bypass.mm 原有 access/stat/fopen 未覆盖这三个系统调用，xkjgaol 可能绕过已有 hook 通过 open() 直接探测越狱路径

### diag: v123 — 新增 success 回调包装 + NSURLConnection hook + 请求体日志
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：`sendVcodeWithParam:` hook 中新增 wrappedSuccess（1-arg block），拦截服务端响应；新增 NSURLConnection sendAsynchronousRequest hook 捕获请求体；NSURLSession hook 中打印 req.HTTPBody
- 原因：需要确认 vcodeType=1 是否真正发到服务器，以及服务端响应内容
- 结果：async 闪退（约 8-12 秒后），根因是 success 回调调用约定不兼容，包装后触发崩溃

### diag: v124 — 改 success 为 2-arg 调用 + NSURLConnection nil 保护
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：wrappedSuccess 改用 `((void(^)(id,id))success)(result, nil)` 双参数调用；NSURLConnection hook 加 nil 判断
- 结果：点击「获取验证码」立即秒退；根因是 NSURLConnection hook 内调 cloudLog 导致 NSURLSession 锁重入崩溃

### fix: v125 — 移除 NSURLConnection hook + 去除 success 包装 + 读 block descriptor
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：删除 NSURLConnection sendAsynchronousRequest hook；`sendVcodeWithParam:` 改回直接传 original success；新增读 block descriptor (`blk[3]`) 记录 `success_sig`
- 原因：NSURLConnection hook 是秒退根因；success 包装两种调用方式均崩溃，暂时放弃
- 结果：不再闪退，但 `success_sig: "nil"`（blk 偏移误用了 blk[4]，正确应为 blk[3]），且仍未收到 SMS

### diag: v126 — 新增 NSURLSessionDataTask resume hook（尝试捕获 delegate 路径请求）
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：新增 `%hook NSURLSessionDataTask - (void)resume` 在内部调 `cloudLog` 记录 `currentRequest`；同时恢复 wrappedSuccess（1-arg）
- 原因：vcode 请求走 delegate-based NSURLSession，completion-handler hook 捕获不到；尝试在 resume 层面抓请求体
- 结果：点击按钮立即崩溃；根因是 ElleKit 无法安全 hook CoreFoundation 桥接类 NSURLSessionDataTask

### diag: v127 — resume hook 改为只用 tlog（去掉 cloudLog）
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：resume hook 内改用 `tlog()` 替代 `cloudLog()`；去掉 wrappedSuccess
- 结果：仍在点击按钮时立即崩溃；确认 NSURLSessionDataTask resume hook 本身就会 crash，与 cloudLog/tlog 无关

### fix: v128 — 彻底移除 NSURLSessionDataTask resume hook
- 文件：`Reverse/output/tweak/Tweak.x`
- 改动：删除整个 `%hook NSURLSessionDataTask` 块
- 原因：ElleKit 无法安全 hook 私有子类（`__NSCFLocalDataTask` 等），hook 本身触发崩溃；放弃从 resume 层面捕获 vcode 请求体
- 结果：不再崩溃；当前状态：vcode 请求发出（服务端无 fail 回调），但手机收不到 SMS，真正根因待查

## 2026-06-16
### fix: v30 — bypass.mm 补 open/openat/statfs/opendir/faccessat 越狱检测 hook
- 文件：`Reverse/output/tweak/bypass.mm`
- 改动：新增 hook_open / hook_openat（jailbreak 路径返回 ENOENT，含 3s 启动窗口日志）；hook_statfs（JB 路径返回 ENOENT，与 spoof.mm 形成链式调用）；hook_opendir（JB 路径返回 NULL/ENOENT）；hook_faccessat（JB 路径返回 ENOENT）；hookEnvDetect() 末尾注册以上 5 个 hook；头文件新增 <sys/mount.h>
- 原因：tlog 确认 xkjgaol 37ms jbprobe 期间无 stat/access_blocked 事件，说明 xkjgaol 用 open/openat/statfs/opendir 检测越狱，而 bypass.mm 原来只 hook 了 stat/access/fopen；导致 _gikeychain_key2 (-25300) 始终未被写入 keychain，xkjgaol 无法生成有效 GTS token，服务端拒绝发 SMS

## 2026-06-18
### diag: v182 — QunarNetSpy 补 didReceiveResponse: + apple.com 日志
- 文件：`Reverse/output/tweak/net_capture.mm` +30/-0
- 改动：新增 `URLSession:dataTask:didReceiveResponse:completionHandler:` 方法到 QunarNetSpy；用 `__block BOOL called` one-shot 保护 cb 防止 double-call（QAPM double-wrap 场景）；对 apple.com URL 记录 `apple_resp`（HTTP status code）；在 `didCompleteWithError:` 新增 apple.com 分支记录 `apple_done`（error/code）
- 原因：tlog 发现优惠券页卡死 586s 前有两次 FraudScoreReport 请求至 bag.itunes.apple.com，需确认是网络 timeout 还是 HTTP 错误；原 didCompleteWithError: 对 HTTP 4xx/5xx 的 e=nil 无感知
